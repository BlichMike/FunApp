import { Typography } from '@mui/material';

export const HomePage: React.FC = () => {
  return (
    <Typography variant="h4" textAlign={'center'}>
      Welcome to Mamush app, enjoy *____*
    </Typography>
  );
};
